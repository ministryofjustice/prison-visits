:css
  table {
    border-collapse: collapse;
  }

  td {
    text-align: right;
    min-width: 2em;
  }

  .prison-name {
    text-align: left;
  }

  .even {
    background-color: LightGray;
  }

%h1
  Confirmations by week
  = link_to('CSV', format: :csv)

%table
  %tr
    %th
      Prison
    - @dataset.week_range.each do |week_no|
      %th
        = @start_of_year + week_no * 7
  - @prisons.each_with_index do |prison, i|
    %tr{class: i.even? ? 'even' : 'odd'}
      %td.prison-name
        = prison
      - @dataset.week_range.each do |week_no|
        %td
          = @dataset.for_prison(prison)[week_no]
