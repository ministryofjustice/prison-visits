Dear <%= @visit.visitors.first.first_name %>,

You have requested the following visiting slots for <%= @visit.visitors.size %> people at HMP <%= @visit.prisoner.prison_name %>:

<% @visit.slots.each_with_index do |slot, index| %>
<% if slot.date.present? %>
<%= index + 1%>. <%= Date.parse(slot.date).strftime("%A %e %B") %>, <%= display_time_slot(slot.times) %>
<% end %>
<% end %>

Your visit is not yet confirmed. You will receive a confirmation email from the prison booking staff within 3 working days.
