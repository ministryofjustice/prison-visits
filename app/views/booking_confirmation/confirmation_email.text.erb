Dear <%= @visit.visitors.first.first_name %>,

YOUR BOOKING REQUEST IS BEING PROCESSED

We have received your visit request. We'll email you within <%= Slot::LEAD_DAYS %> working days to confirm a date and time. Please don't contact us sooner than <%= Slot::LEAD_DAYS %> working days, as we won't be able to comment on booking requests that are being processed.

You have requested the following dates and times for <%= pluralize(@visit.visitors.size, "person") %> at HMP <%= @visit.prisoner.prison_name %>:

<% @visit.slots.each_with_index do |slot, index| %>
<% if slot.date.present? %>
<%= index + 1%>. <%= Date.parse(slot.date).strftime("%A %e %B") %>, <%= display_time_slot(slot.times) %>
<% end %>
<% end %>

If you have any special requirements, eg wheelchair access, or you want to cancel or make a change to your request, please reply to this email.
