You received the following booking to visit HMP XXX on one of the following dates:

<% @visit.slots.each_with_index do |slot, index| %>
<% if slot.date.present? %>
<%= index + 1%>. <%= Date.parse(slot.date).strftime("%A %e %B") %>, <%= display_time_slot(slot.times) %>
<% end %>
<% end %>

Please check the chosen dates on Prison NOMIS, topmost is the first preference.

* If one of them is available and the visitor(s) exist on the prisoner’s contact list, then please enter the visit on Prison NOMIS. Next, email the visitor to confirm the day and time. Use the template XXX to send this email.

* If no dates are available, email the visitor to ask them to go back to the prison visits booking website and choose three new dates. They can also telephone the prison to book a date. Use the template XXY to send this email.


Prisoner's details
==================

First name: <%= @visit.prisoner.first_name %>
Last name: <%= @visit.prisoner.last_name %>
Number: <%= @visit.prisoner.number %>
Prison: <%= @visit.prisoner.prison_name %>
DOB: <%= @visit.prisoner.date_of_birth.strftime("%e %B %Y") %>

<% @visit.visitors.each_with_index do |visitor, index| %>

Visitor <%= index + 1 %>
=========
First name: <%= visitor.first_name %>
Last name: <%= visitor.last_name %>
<% if visitor.date_of_birth %>
DOB: <%= visitor.date_of_birth.strftime("%e %B %Y") %>
<% end %>
<% if visitor.email %>
Email: <%= visitor.email %>
<% end %>
<% if visitor.phone %>
Phone: <%= visitor.phone %>
<% end %>
<% if visitor.type %>
Adult/child: <%= visitor.type %>
<% end %>
<% end %>
